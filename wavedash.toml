# Official Wavedash CLI config: https://docs.wavedash.com/cli/configuration
[project]
name    = "Pacific Rhythm"
game_id = "pacific-rhythm"

upload_dir = "./dist"

[custom]
version = "0.1.0"
# Vite emits the main bundle as dist/game.js (see vite.config.ts).
entrypoint = "game.js"
